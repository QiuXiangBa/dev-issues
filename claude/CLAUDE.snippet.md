## 问题知识库

`~/dev-issues` 是跨项目共享的问题与解决方案知识库，通过 `/issues` Skill 使用。

- 遇到报错、依赖冲突、API 行为异常等调试问题时，先执行 `/issues search <关键词>` 看有没有已记录的解决方案，再开始排查。
- 一个非平凡的问题被解决后，主动提议执行 `/issues add` 记录，由用户确认后再写入。
- 「非平凡」指：花了超过几轮对话才解决、原因不明显、或者其他项目很可能再遇到。
